---
import Layout from '../../layouts/Layout.astro';
import Head from '../../components/layout/Head.astro';
import Header from '../../components/layout/Header.astro';
import Footer from '../../components/layout/Footer.astro';
import EventRegistration from '../../components/islands/EventRegistration';
import Card from '../../components/ui/Card.astro';
import Badge from '../../components/ui/Badge.astro';
import Button from '../../components/ui/Button.astro';
import { sanity } from '../../lib/sanity';
import { createPageUrl } from '../../lib/utils';
import { DEFAULT_EVENTS, type SignatureEvent } from '../../lib/defaultContent';

const events = sanity ? await sanity.fetch(`*[_type == "signatureEvent"] | order(order asc, _createdAt desc)`).catch(() => []) : [];
const heroEvents = sanity ? await sanity.fetch(`*[_type == "hero" && page == "events"][0]{ heading, subheading }`).catch(() => null) : null;

const displayEvents: SignatureEvent[] = events.length > 0 ? events : DEFAULT_EVENTS;
---

<Layout>
  <Head slot="head" title="Signature Events" description="Join LEAP Association signature events designed to transform your leadership practice." />
  <Header />
  <main id="main-content" class="min-h-screen bg-gradient-to-br from-slate-50 to-white pt-24 pb-16">
    <div class="max-w-7xl mx-auto px-6 lg:px-8">
      <!-- Hero Section -->
      <div class="text-center mb-16">
        <span class="block text-primary font-semibold text-sm tracking-wider uppercase mb-4">
          Signature Events
        </span>
        <h1 class="text-4xl lg:text-5xl font-bold text-primary mb-6">
          {heroEvents?.heading ?? 'Transformative Leadership Experiences'}
        </h1>
        <p class="text-xl text-slate-600 max-w-3xl mx-auto">
          {heroEvents?.subheading ?? "Join our signature events designed to deepen your practice and connect with other leaders committed to excellence."}
        </p>
      </div>

      <!-- Four Event Cards -->
      <div class="grid md:grid-cols-2 gap-8 mb-16">
        {displayEvents.map((event: any) => (
          <Card class="p-8 hover:shadow-xl transition-all group overflow-hidden relative">
            {event.badge && (
              <span class={`absolute top-4 right-4 px-3 py-1 ${event.badgeColor || 'bg-primary'} text-white text-sm font-bold rounded-full`}>
                {event.badge}
              </span>
            )}

            <div class={`w-16 h-16 rounded-2xl bg-gradient-to-br ${event.color || 'from-primary to-primary-dark'} flex items-center justify-center mb-6 shadow-lg group-hover:scale-110 transition-transform`}>
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d={event.icon} />
              </svg>
            </div>

            <h2 class="text-2xl font-bold text-primary mb-2">{event.title}</h2>
            <p class="text-accent font-semibold mb-4">{event.subtitle}</p>

            <!-- Event Details -->
            <div class="flex flex-wrap gap-4 mb-6 text-sm">
              <div class="flex items-center gap-2 text-slate-600">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
                {event.duration}
              </div>
              <div class="flex items-center gap-2 text-slate-600">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                </svg>
                {event.format}
              </div>
              {event.capacity && (
                <div class="flex items-center gap-2 text-slate-600">
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z" />
                  </svg>
                  {event.capacity}
                </div>
              )}
            </div>

            {event.price && (
              <p class="text-sm text-emerald-600 font-semibold mb-4">{event.price}</p>
            )}

            <!-- Features -->
            <div class="mb-6">
              <h4 class="font-semibold text-slate-900 mb-3">What's Included:</h4>
              <ul class="space-y-2">
                {(event.features || []).slice(0, 4).map((feature: string) => (
                  <li class="flex items-start gap-2 text-sm text-slate-600">
                    <svg class="w-4 h-4 text-emerald-500 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                    </svg>
                    {feature}
                  </li>
                ))}
              </ul>
            </div>

            <!-- Benefits -->
            <div class="mb-6">
              <h4 class="font-semibold text-slate-900 mb-3">Benefits:</h4>
              <div class="flex flex-wrap gap-2">
                {(event.benefits || []).map((benefit: string) => (
                  <span class="px-3 py-1 bg-slate-100 text-slate-700 text-xs rounded-full">
                    {benefit}
                  </span>
                ))}
              </div>
            </div>

            {event.special && (
              <div class="mb-6 p-3 bg-emerald-50 border border-emerald-200 rounded-lg">
                <p class="text-emerald-700 font-semibold text-sm flex items-center gap-2">
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
                  </svg>
                  {event.special}
                </p>
              </div>
            )}

            {event._id ? (
              <EventRegistration client:load eventId={event._id} />
            ) : (
              <Button href={createPageUrl('Contact')} class="w-full">
                Inquire About This Event
              </Button>
            )}
          </Card>
        ))}
      </div>

      <!-- CTA Section -->
      <div class="bg-gradient-to-br from-primary to-primary-dark rounded-3xl p-6 sm:p-10 lg:p-16 text-white relative overflow-hidden">
        <div class="absolute -top-10 -right-10 w-64 h-64 bg-white/10 rounded-full blur-3xl" />
        <div class="absolute -bottom-10 -left-10 w-80 h-80 bg-accent/10 rounded-full blur-3xl" />

        <div class="relative z-10 grid lg:grid-cols-2 gap-12 items-center">
          <div>
            <h2 class="text-3xl lg:text-4xl font-bold mb-4">
              Ready to Transform Your Leadership Practice?
            </h2>
            <p class="text-slate-200 text-lg mb-6">
              Not sure which event is right for you? Let's talk about your goals
              and find the perfect fit for your team.
            </p>
            <div class="flex flex-col sm:flex-row gap-4">
              <a href={createPageUrl('ScheduleCall')} class="inline-block px-8 py-4 bg-white text-primary rounded-xl font-semibold hover:bg-slate-100 transition-colors text-center">
                Schedule a Call
              </a>
              <a href={createPageUrl('Contact')} class="inline-block px-8 py-4 bg-white/10 border-2 border-white/30 text-white rounded-xl font-semibold hover:bg-white/20 transition-colors text-center">
                Contact Us
              </a>
            </div>
          </div>

          <div class="bg-white/10 backdrop-blur-sm border border-white/20 rounded-2xl p-8">
            <h3 class="text-xl font-bold mb-4 flex items-center gap-2">
              <svg class="w-6 h-6 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
              </svg>
              LEAP Lounge
            </h3>
            <p class="text-slate-200 mb-4">
              Experience our premier event venue in Atlanta. Perfect for workshops, meetings, and team experiences.
            </p>
            <a href={`${import.meta.env.BASE_URL}events/lounge`} class="text-accent font-semibold hover:text-accent-light transition-colors inline-flex items-center gap-2">
              Learn More About LEAP Lounge
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
              </svg>
            </a>
          </div>
        </div>
      </div>
    </div>
  </main>
  <Footer />
</Layout>

